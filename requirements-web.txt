# Runtime dependencies for the Arda Link WEB SERVICE (the container image).
#
# Deliberately narrower than requirements.txt: the web service never runs batch
# work, so it does not need earthengine-api, osmium, geopandas, pyproj, staticmap
# or the weight-formula/GEE export tooling. Those live in requirements.txt and run
# on the export machine / GitHub Actions (see .github/workflows).
#
# Keep this list honest: it is derived from the imports under app/ (verify with
#   grep -rhE '^\s*(import|from) ' app/ | sort -u
# ). Adding a library here adds it to every deployed container.

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Postgres / Supabase
psycopg[binary]==3.2.1
supabase==2.9.0

# Object storage (COG reads at request time)
boto3==1.35.30

# Raster + geometry reads (rasterio ships its own GDAL wheels)
rasterio==1.3.11
shapely==2.0.6
numpy

# Map rendering + HTTP + config
Pillow==10.4.0
httpx==0.27.2
PyYAML==6.0.2
